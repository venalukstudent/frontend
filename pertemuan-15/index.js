// Module
import { fullName, numbers, sayGreetings } from "./utils.js";
import sayGreetings from "./utils.js";

// gunakan as untuk penggantian atau yang di sebut alias contohnya fullname as nama, lalu bagian console.log bisa di ganti dengan nama
// untuk import dan export
console.log(fullName);
console.log(numbers);
console.log(sayGreetings());
